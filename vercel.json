{
  "version": 2,
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/nestjs",
      "config": {
        "includeFiles": ["dist/**", "node_modules/@prisma/**", "node_modules/.prisma/**"]
      }
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "api/index.ts"
    }
  ]
}
